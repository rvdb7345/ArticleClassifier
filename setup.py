from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Project to use graph neural networks and label-guided embeddings to improve article classification',
    author='Robin van den Berg',
    license='MIT',
)
